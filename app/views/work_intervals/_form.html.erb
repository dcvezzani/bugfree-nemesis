<%= form_for(@work_interval, url: @url) do |f| %>
  <% if @work_interval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_interval.errors.count, "error") %> prohibited this work_interval from being saved:</h2>

      <ul>
      <% @work_interval.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_id %><br />
    <%= f.number_field :entry_id %>
  </div>
  <div class="field">
    <%= f.label :started_at %><br />
    <%= f.text_field :started_at, value: in_time_zone(@work_interval.started_at) %>
  </div>
  <div class="field">
    <%= f.label :ended_at %><br />
    <%= f.text_field :ended_at, value: in_time_zone(@work_interval.ended_at) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script type="text/javascript">

  $(document).ready(function(){
    // $( "#work_interval_started_at" ).timepicker();
    // $( "#work_interval_started_at" ).datepicker( "option", "dateFormat", "yy-mm-dd" );
    $('#work_interval_started_at').timepicker()

    // $( "#work_interval_ended_at" ).timepicker();
    // $( "#work_interval_ended_at" ).datepicker( "option", "dateFormat", "yy-mm-dd" );
    $('#work_interval_ended_at').timepicker()

  });
</script>
