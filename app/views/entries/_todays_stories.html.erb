  <% entry.todays_stories.each do |story| %>
    <li><%= link_to "(#{format_date(story.due_on)}) #{story.title} : [#{story.hours_est} / #{story.hours_worked} / #{story.hours_todo}]", [project, story] %>
    <% if story.is_open? %>
    | <%= link_to 'Mark as Done', [:mark_as_done, project, story], method: :put, data: { confirm: 'Are you sure?' } %>
    <% end %>
    </li>
  <% end %>

