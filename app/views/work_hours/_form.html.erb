<%= form_for(@work_hour, url: @options[:url]) do |f| %>
  <% if @work_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_hour.errors.count, "error") %> prohibited this work_hour from being saved:</h2>

      <ul>
      <% @work_hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="">
    <%= f.label :project_id %><br />
    <%= f.text_field :project_id %>
  </div>
  <div class="field" style="">
    <%= f.label :item_id %><br />
    <%= f.text_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :hours %><br />
    <%= f.text_field :hours %>
  </div>
  <div class="field">
    <%= f.label :entry_story %><br />
    <%= f.text_field :entry_story %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
