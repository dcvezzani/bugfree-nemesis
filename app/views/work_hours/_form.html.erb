<%= form_for(@work_hour, url: @options[:url]) do |f| %>
  <% if @work_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_hour.errors.count, "error") %> prohibited this work_hour from being saved:</h2>

      <ul>
      <% @work_hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :render_as_partial, true %>

  <div class="field">
    <span <%= hide_if?(@options[:quiet]) %>><%= f.label :hours %></span><br />
    <%= f.text_field :hours %>
  </div>
  <div class="field">
    <span <%= hide_if?(@options[:quiet]) %>><%= f.label :note %></span><br />
    <%= f.text_area :note %>
  </div>

  <div class="actions">
    <%= f.submit %>
    <%= button_tag("Cancel", name: "cancel", type: 'button') %>
  </div>
<% end %>
