<h1>Listing work_hours</h1>

<table>
  <tr>
    <th>Hours</th>
    <th>Note</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @work_hours.each do |work_hour| %>
  <tr>
    <td><%= work_hour.hours %></td>
    <td><%= work_hour.note %></td>
    <td><%= link_to 'Show', [@project, @story, work_hour] %></td>
    <td><%= link_to 'Edit', [:edit, @project, @story, work_hour] %></td>
    <td><%= link_to 'Destroy', [@project, @story, work_hour], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work hour', [:new, @project, @story, :work_hour] %>
